______________________________________________________________________________________________________________________________
			### FUTURE IMPROVEMENTS ###
______________________________________________________________________________________________________________________________
# Сделать третий статус для SLA - прошло 70% + уведомление об этом ответственному сотруднику.

# Отказался от Вложений (файлов):
Для диплома:
" В самой системе help desk реализована текстовая коммуникация.
Модуль вложения является точкой расширения для последующих итераций.
В данный момент файлы могут направляться на почту исполнителя при необходимости".

# При создании обращения реализовать созранение черновика вручную 
или автоматически при закрытии страницы обращения/переключению на другую вкладку.

# Сделать динамическую форму при выборе Раздела и направления обслуживания 
(создать для каждого направл. обсл. свою форму или динамически менять при выборе на текущей странице).
Потребуется переписать фронт на React.

- Для начала можно прикрутить в сущность Ticket несколько фиксированных полей.
А на фронтенде по-простому, через if/else скрывать или показывать эти поля в зависимости от выбранного направления.

ALTER TABLE tickets ADD COLUMN extra_text1 VARCHAR(500);
ALTER TABLE tickets ADD COLUMN extra_text2 VARCHAR(500);
ALTER TABLE tickets ADD COLUMN extra_int1 INTEGER;

# Сделать Хлебные крошки при выборе раздела и направления.

# Добавить функционал для администраторов создавать разделы и направления обслуживания.

# Добавить возможность возобновления обращения, а следовательно и статус "Закрыто" (CLOSED).